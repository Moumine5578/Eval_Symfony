{% extends 'base.html.twig' %}

{% block title %}Modifier Relevés{% endblock %}

{% block body %}
    <h1>Modifier le Relevé</h1>

    {{ include('releves/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a class="btn btn-success" href="{{ path('app_releves_index') }}">Liste</a>

    {{ include('releves/_delete_form.html.twig') }}

    <style>
        .btn-success {
            margin-bottom: 10px;
        }

        .btn-primary {
            margin-bottom: 10px;
        }
    </style>

{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Releves index{% endblock %}

{% block body %}
    <h1>Relevés</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Lieu</th>
                <th>Relevé Brut</th>
                <th>Tableau</th>
                <th>Visualisation</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for relefe in releves %}
            <tr>
                <td>{{ relefe.date ? relefe.date|date('Y-m-d') : '' }}</td>
                <td>{{ relefe.lieu.nom }}</td>
                <td>{{ relefe.releveBrut }}</td>
                <td>{{ relefe.releveBrut }}</td>
                <td>{{ relefe.releveBrut }}</td>
                <td>
                    <a href="{{ path('app_releves_show', {'id': relefe.id}) }}">voir</a>
                    <a href="{{ path('app_releves_edit', {'id': relefe.id}) }}">modifier</a>
                    <a href="{{ path('app_releves_delete', {'id': relefe.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_releves_new') }}">Create new</a>
{% endblock %}

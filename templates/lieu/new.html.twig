{% extends 'base.html.twig' %}

{% block title %}New Lieu{% endblock %}

{% block body %}
    <h1>Ajouter un Lieu</h1>

    {{ include('lieu/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a class="btn btn-success" href="{{ path('app_lieu_index') }}">liste</a>

    <style>
        .btn-success {
            margin-top: 10px;
        }
    </style>
{% endblock %}
